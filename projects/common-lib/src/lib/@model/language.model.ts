export class Language {
  code: string
  symbol: string
  name: string

  constructor(code: string, symbol: string, name: string){
    this.code = code
    this.symbol = symbol
    this.name = name
  }
}
